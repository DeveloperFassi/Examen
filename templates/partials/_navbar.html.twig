<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('app_home') }}">Car Location Lyon</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="#"> <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    BackOffice Management
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('rental_index') }}">Liste des locations</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('driver_index') }}">Liste des conducteurs</a>
                    <a class="dropdown-item" href="{{ path('vehicle_index') }}">Liste des véhicules</a>
                </div>
            </li>
        </ul>
        <form class="form-inline my-2 my-lg-0" method="post" action="{{ path('vehicle_index') }}">
            <input class="form-control mr-sm-2" name="vehicule_search" placeholder="Rechercher un véhicule...">
            <button class="btn btn-success my-2 my-sm-0">Search</button>
        </form>
        <form class="form-inline my-2 my-lg-0" method="post" action="{{ path('driver_index') }}">
            <input class="form-control mr-sm-2" name="driver_search" placeholder="Rechercher un conducteur...">
            <button class="btn btn-success my-2 my-sm-0">Search</button>
        </form>
    </div>
</nav>
